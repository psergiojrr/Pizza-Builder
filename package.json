{
  "name": "pizza-monorepo",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@pizza/types": "1.0.0",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ]
  },
  "scripts": {
    "dev:api": "yarn workspace pizza-api dev",
    "build:api": "yarn workspace pizza-api build",
    "start:api": "yarn workspace pizza-api start",
    "start:client": "yarn workspace client start"
  }
}
