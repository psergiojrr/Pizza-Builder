version: "3.9"
services:
  api:
    build: ./apps/api
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    develop:
      watch:
        - action: sync
          path: ./apps/api/src
          target: /app/src
        - action: rebuild
          path: ./apps/api/package.json
        - action: rebuild
          path: ./apps/api/tsconfig.json
